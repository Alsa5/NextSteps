Place your licensed Maratype.woff2 file in this folder.

The app loads it via @font-face in src/index.css. Until the file is present, Orbitron is used as the fallback — it is a similar geometric display face.

Download or obtain Maratype from Marathon / your brand font package, then name it exactly:

  Maratype.woff2
